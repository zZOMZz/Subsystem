import { ModalTable, ModalForm } from "@/components";
const model: React.FC = () => {
    return (
        <>
            <ModalForm />
            <ModalTable />
        </>
    );
}

export default model;