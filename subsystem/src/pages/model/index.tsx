import { LogTable, ModalForm } from "@/components";
const model: React.FC = () => {
    return (
        <>
            <ModalForm />
            <LogTable />
        </>
    );
}

export default model;