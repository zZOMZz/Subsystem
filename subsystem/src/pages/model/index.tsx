import { LogTable, DefaultForm } from "@/components";
const model: React.FC = () => {
    return (
        <>
            <DefaultForm />
            <LogTable />
        </>
    );
}

export default model;