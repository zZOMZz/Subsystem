import { LogTable, DefaultForm } from "@/components";
const sample: React.FC = () => {
    return (
        <>
            <DefaultForm />
            <LogTable />
        </>
    );
}

export default sample;